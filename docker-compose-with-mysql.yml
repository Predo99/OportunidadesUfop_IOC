version: '2'

services:
  oportunidades:
    tty: true
    build: ./src/laravel
    image: laravel-oportunidades:latest
    container_name: oportunidades
    volumes:
      - /var/www/laravel-oportunidades/project:/app
      - /var/www/laravel-oportunidades/src/laravel/ssl/certs:/etc/apache2/certs
      - /var/www/laravel-oportunidades/src/laravel/ssl/private:/etc/apache2/private
      # - /etc/pki/tls/certs:/etc/apache2/certs
      # - /etc/pki/tls/private:/etc/apache2/private
    ports:
      - "8084:80"
      - "8085:443"
    restart: always
    networks: 
      default:
        ipv4_address: 172.20.0.4
    depends_on:
      - database
  database:
    image: mysql:5.7
    container_name: mysql57
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=homestead
    networks: 
      default:
        ipv4_address: 172.20.0.5
    volumes: 
      - database
networks:
  default:
    external:
      name: laravel
volumes: 
  database: